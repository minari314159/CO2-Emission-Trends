[project]
name = "co2-emission-trends"
version = "0.1.0"
description = "The following data includes the breakdown of emissions: total greenhouse gases, carbon dioxide, methane, and nitrous oxide individually, by sector and country. "
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.8",
    "pandas>=3.0.1",
    "seaborn>=0.13.2",
]
